import { Shell } from '@/components/layout/Shell'
import { Placeholder } from '@/components/ui/Placeholder'

export default function Page() {
  return (
    <Shell title="Set Up Your Profile">
      <Placeholder note="Provider onboarding — profile and service packages — arrives in the Week 5-6 sprint." />
    </Shell>
  )
}
